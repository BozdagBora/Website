# Bora Bozdag — Static Website Demo

This repository contains a static portfolio-style website that emphasizes readable simulation reports.

## What's included

- `index.html`: Main landing page with about, experience, and report sections.
- `styles.css`: Minimal editorial styling inspired by modern academic portfolio layouts.
- `reports/*.html`: Clickable static report pages replacing interactive simulations.

## Run locally

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000.

## Push to GitHub

```bash
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin work
```

Open a pull request from `work` to your default branch.
